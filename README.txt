To run smallsh.c
    1. Compile with gcc --std=gnu99 -o smallsh smallsh.c 
    2. Run program as ./smallsh 
    3. Program will run in the terminal window
    4. Run built-in command "exit" to quit the program

    
